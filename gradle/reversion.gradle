def loadReVersion() {
    File file = new File("${project.buildDir}/.git")
    if (!file.exists()) {
        return ''
    }
    def grgit  = Grgit.open()
    return  grgit.head().getAbbreviatedId()
}
ext.reversion = loadReVersion()
