def loadVersion() {
    File file = new File("${rootProject.rootDir}/.version")
    if (!file.exists()) {
        return '0.0.999'
    }
    def pipelineId = System.getenv('PIPELINE_ID')
    return file.text + '.' + (pipelineId ? pipelineId : '0')
}

version = loadVersion()