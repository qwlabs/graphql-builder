pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "com.jfrog.bintray" version "${bintrayPluginVersion}"
    }
}

rootProject.name = 'graphql-builder'
